#pragma once

#include<string>
#include<vector>

using namespace std;

class Helper {
public:
	static string combine_list_tag_to_single_string(vector<string> listTag);
	static void Helper::combine_listNote_listTag(vector<Tags>& listTag, vector<Note>& listNote);
};
